@ = src 폴더
{} = 구조분해할당

{}이걸로 감싸 있으면 body / 그 앞이 헤드

==무조건 헤드와 바디로 구성==
ㄴ헤드= **이름** / 바디= **{} 안**
ㄴ헤드가 없는 건 **이름 지정이 필요 없다**(= 하나라서 지정 x)는 뜻으로 **헤드생략**
ㄴ헤드가 없으면 바디를 {}로 묶어 줄 필요 없음  => @ 뒤가 바디 이름

**sync= 동기** =>디폴트, 생략
ㄴ> 값이 입력 되자마자 할당
**async= 비동기**

? = 참이면 앞에 거 아니면 뒤
?? = 반환 ---> A가 null이든 undefined든 비어만 있으면 B로 반환
?: = null 허용
?. = null이어도 상관없으니 B를 띄우란 뜻