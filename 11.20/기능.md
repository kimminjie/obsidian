deprecated = 사용 가능하지만 곧 없어질 예정이라 쓰지 말라고 표시된 기능

리액트에서 기능(함수) = 훅(=모양이 상관 없음), 람다로도 변경 가능/ 자바에선 메소드
ㄴ>use가 있으면 훅
ㄴ>use 없으면 핸들러 =>반드시 콜백이 됨.
-핸들은 스트링만 가질 수 있음
-핸들이 화면이니까
-사람이 입력하는 건 int(=var)가 아니라 다 string임

람다=훅=어나니무스(익명)