**큐(Queue)
FIFO (First In, First Out)= 먼저 들어온 데이터가 먼저 나가는 구조**

**스택 (Stack)
LIFO (Last In, First Out)= 나중에 들어온 데이터가 먼저 나가는 구조**

힙(Heap)


api는 run으로 실행 시켰음
ui는 pnpm run dev (npm run dev를 터미널에 치면 cli로 내가 직접 npm에게 커맨드할 수 있음) --> npm 안 좋음 pnpm으로 하기

스프링은 빈(Bean) 객체라 POJO(Plain Old Java Object) 사용 안 함

