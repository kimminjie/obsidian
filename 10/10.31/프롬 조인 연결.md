조인은 부모(PK)와 자식(FK)을 연결해야 함.

pk들이 
==ERD 짤 때 키만 갖고 있는 애를 만들어 줌== = fk를 많이 갖고 있는 애
![[Pasted image 20251031113358.png]] 이런식으로

**13번 문제 프롬** ㄱ
from player p
join team t
    on p.team_id = t.team_id
join schedule sch
    on t.team_id = sch.hometeam_id or t.team_id = sch.awayteam_id
join stadium s
    on sch.stadium_id = s.stadium_id
 